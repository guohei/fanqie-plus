description = "Continue the next Fanqie chapter"
prompt = """
Use `skills/fanqie-plus/SKILL.md` as the source of truth. If this adapter conflicts with the skill, follow the skill.

Continue the next chapter. Hard limit: one chapter only; do not batch; do not auto-continue.

User input:
{{args}}
"""
